spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  datasource:
    driver-class-name: org.postgresql.Driver
git:
  scheduler-enabled: false
  skip-first-execution: true
  repo-root: src/test/resources
